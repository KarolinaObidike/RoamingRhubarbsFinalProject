AWSTemplateFormatVersion: "2010-09-09"

Description: Deployment bucket for ETL Lambda package

Resources:

  DeploymentBucket:
    Type: AWS::S3::Bucket


Outputs:

  DeploymentBucketName:
    Description: Name of the deployment bucket
    Value: !Ref DeploymentBucket